# Server configuration
PORT=5000
NODE_ENV=production

# If using a database for persistent storage, uncomment and configure the following:
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name